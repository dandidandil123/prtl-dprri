services:
  - type: web
    name: dpr-api-server
    env: node
    buildCommand: npm install && npm run setup
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000